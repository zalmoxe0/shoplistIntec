CREATE TABLE `eshopliststdb`.`new_table`
(
    `Product`     INT         NOT NULL AUTO_INCREMENT,
    `name`        VARCHAR(45) NOT NULL,
    `description` VARCHAR(45) NOT NULL,
    `price`       VARCHAR(45) NOT NULL,
    PRIMARY KEY (`Product`)
);
